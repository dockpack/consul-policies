---

consul_user: "consul"
consul_group: "bin"
consul_config_path: "/etc/consul"
consul_configd_path: "{{ consul_config_path }}/consul.d"
consul_policies: "/home/{{ consul_user }}/policies"
consul_requests: "/home/{{ consul_user }}/acl_token_requests"
